<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unicorn.csp.xcdemo">

    <uses-sdk tools:overrideLibrary="uk.co.imallan.jellyrefresh" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:allowBackup,android:theme">

        <activity android:name=".activity.shared.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.shared.LoginActivity" />


        <activity android:name=".activity.technician.MainActivity" />
        <activity
            android:name=".activity.technician.SuspendActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity android:name=".activity.LLActivity" />
        <activity android:name=".activity.OperationActivity" />
        <activity android:name=".activity.OverActivity" />
        <activity
            android:name=".activity.technician.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
    </application>

</manifest>
