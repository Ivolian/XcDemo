<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unicorn.csp.xcdemo">

    <uses-sdk tools:overrideLibrary="uk.co.imallan.jellyrefresh" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SimpleApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:allowBackup,android:theme">

        <!-- shared -->
        <activity android:name=".activity.shared.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.shared.LoginActivity" />

        <!-- technician -->
        <activity android:name=".activity.technician.WorkOrderActivity" />
        <activity
            android:name=".activity.technician.WorkOrderDetailActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.shared.SuspendActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.technician.PackActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.technician.OperationActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.technician.AchieveActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.technician.PhotoConfirmActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />

        <!-- chief -->
        <activity android:name=".activity.chief.TodoActivity" />
        <activity
            android:name=".activity.chief.AssignActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />
        <activity
            android:name=".activity.chief.ReviewActivity"
            android:theme="@style/AppTheme.NoActionBar.NoWindow" />


    </application>

</manifest>
